# Browser automation
zendriver>=0.4.0

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Validación
pydantic>=2.7.0

# Base de datos
aiosqlite>=0.20.0

# Procesamiento de imágenes (iconos del scraper kirilloid)
pillow>=10.0.0

# Parseo HTML (parsers de overview de Travian)
beautifulsoup4>=4.12

# Cifrado de contraseñas (Fernet — AES-128-CBC + HMAC-SHA256)
cryptography>=42.0.0

# Validación de email para Pydantic EmailStr (CreateAccountRequest / UpdateAccountRequest)
email-validator>=2.0.0

# Optimización multi-objetivo (simulador de combate — optimizador NSGA-II)
pymoo>=0.6.0

# Estadística (tests de curvatura K-S en human_click)
scipy>=1.11.0

# Tests
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Arquitectura — frontera hexagonal verificable (core no depende de adapters)
# Config en .importlinter (raíz). Ejecutar con: lint-imports
import-linter>=2.0
